# Final Project — Attack Detection (Code Branch)

This branch contains the **implementation only** for the agentic LLM red-teaming framework.

- **Source code:** [`code/`](code/)
- **Setup and usage:** see [`code/README.md`](code/README.md)
- **Paper (LaTeX):** on the paper branch (`main` / `Michaels_paper_fixes`), not on this branch

## Quick start

```bash
cd code
pip install -e ".[dev]"
pytest
```
